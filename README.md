# Hello World Node.js

Un petit serveur Node.js qui affiche une page "Hello World" élégante.

## Installation et lancement

```bash
npm install
npm start
```

Le serveur démarre sur [http://localhost:3000](http://localhost:3000) et sert une page statique stylée depuis `public/index.html`.
